name: Build iOS on merge to main

on:
  push:
    branches: ['main']
    paths: ['app/**']

jobs:
  build_ios:
    name: Build iOS (development)
    type: build
    params:
      platform: ios
      profile: development
